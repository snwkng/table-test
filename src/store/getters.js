export default {
    USERS(state) {
        return state.users;
    },
    CURRENT_PAGE(state) {
        return state.currentPage;
    },
    TOTAL_USERS(state) {
        return state.totalUsers;
    },
    LIMIT_PER_PAGE(state) {
        return state.limitPerPage;
    }
}
